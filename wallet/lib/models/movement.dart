class Movement {
  String title;
  DateTime fecha;
  num amount;
  bool isExpense;

  Movement(
      {required this.title,
      required this.fecha,
      required this.amount,
      required this.isExpense});
}
